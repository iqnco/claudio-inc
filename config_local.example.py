# Template — copy to config_local.py and fill in.
# Personal (non-secret) config, imported by config/settings.py and bot.py.

# Your name — used in the CIO agent's and bot's system prompts.
OWNER = "there"

# Your numeric Telegram chat ID. Used two ways: bot.py only responds to
# messages from this ID (allowlist), and agents can push analysis alerts
# to it directly. Get yours by messaging @userinfobot on Telegram.
TELEGRAM_CHAT_ID = "0"
